config:
  target: "http://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 10

scenarios:
  - name: "Retrieve data"
    flow:
      - get:
          url: "/qa/questions?product_id=37316"
